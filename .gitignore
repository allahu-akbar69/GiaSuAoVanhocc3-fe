# Gradle cache & build outputs
.gradle/
build/
*/build/
.cxx/
.externalNativeBuild/

# Android build artifacts
*.apk
*.ap_
*.aab
captures/

# IDE
.idea/
*.iml

# Machine-local config
local.properties

# Local JDK (không nên commit)
jdk-*/

# Keystore & secrets (GIỮ RIÊNG, KHÔNG COMMIT)
*.jks
*.keystore
*.keystores
**/keystore/**
signing.properties
*.pem
*.p12
*.key

# Firebase/Google services (GIỮ RIÊNG, KHÔNG COMMIT)
**/google-services.json
# Nếu dùng iOS tương ứng (đề phòng)
**/GoogleService-Info.plist

# Logs
*.log

# OS junk
.DS_Store
Thumbs.db

# Editors
.vscode/
*.swp
*.swo

# Env files (nếu dùng)
.env
.env.*

# Local secrets (KHÔNG COMMIT)
secrets.properties

# Crashlytics/Fabric legacy (nếu dùng)
**/fabric.properties
**/crashlytics-build.properties

# Heap dumps & merge leftovers
*.hprof
*.orig
*.rej


